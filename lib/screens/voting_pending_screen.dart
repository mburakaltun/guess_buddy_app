import 'package:flutter/material.dart';

class VotingPendingScreen extends StatelessWidget {
  const VotingPendingScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Home Screen'));
  }
}
